일반 지침
편집
• C++ 및 -Wall -Wextra -Werror 플래그를 사용하여 코드를 컴파일합니다.
• -std=c++98 플래그를 추가하면 코드가 컴파일되어야 합니다.
형식 및 명명 규칙
• 연습 파일의 이름은 다음과 같습니다: ex00, ex01, ..., exn
• 지침에 지정된 대로 파일, 클래스, 함수, 멤버 함수 및 속성의 이름을 지정합니다.
• 클래스 이름을 UpperCamelCase 형식으로 작성하세요. 클래스의 코드를 포함하는 파일은 후자의 이름을 갖습니다. 예를 들어 :
ClassName.hpp/ClassName.h, ClassName.cpp 또는 ClassName.tpp.
따라서 헤더 파일에 "BrickWall" 클래스 정의가 포함되어 있으면 해당 클래스는
이름은 BrickWall.hpp가 됩니다.
• 달리 지정하지 않는 한 모든 메시지는 반환으로 끝나야 합니다.
행에 추가하여 표준 출력에 표시됩니다.
• 챠오 노르미네트! C++ 모듈 중에는 표준이 적용되지 않습니다. 원하는 스타일을 따를 수 있습니다. 하지만 동료가 이해할 수 없는 코드는 동료가 평가할 수 없는 코드라는 점을 명심하세요. 따라서 깨끗하고 읽기 쉬운 코드를 생성하기 위해 최선을 다하십시오.
허용되는 것과 허용되지 않는 것 C 언어는 이제 끝났습니다. 이제 C++를 시작할 시간입니다! 그러므로 :
• 거의 전체 표준 라이브러리를 사용할 수 있습니다. 그래서
익숙한 환경에 머물기보다는 가능한 한 버전을 사용하려고 노력하세요.
C++ C 함수는 익숙합니다.
• 단, 다른 외부 라이브러리는 사용할 수 없습니다.
이는 C++11(및 파생물) 및 Boost 세트가 금지됨을 의미합니다. 또한,
특정 기능은 여전히 ​​금지되어 있습니다. *printf(), *alloc() 및 free() 함수를 사용하면 점수는 0이 됩니다.
• 별도로 명시하지 않는 한 네임스페이스 <ns_name>을 사용하는 키워드는
그리고 친구는 금지되어 있습니다. 이를 사용하면 -42점을 받게 됩니다.
• 귀하에게는 모듈 08 및 09의 STL만 사용할 수 있습니다. 그때까지는
컨테이너(벡터/목록/맵/등) 및 알고리즘(모든 것)의 사용
<알고리즘> 포함 필요)은 금지됩니다. 그렇지 않으면, 당신은 얻을 것이다
점수는 -42입니다.
일부 설계 의무
• 메모리 누수는 C++에도 존재합니다. 메모리를 할당할 때
(new 키워드 사용) 메모리 누수가 없어야 합니다.
• 모듈 02부터 모듈 09까지, 달리 명시적으로 지정하지 않는 한 클래스는 Coplian 형식이라는 표준 형식을 준수해야 합니다.
• 헤더 파일에 구현된 함수(함수인 경우는 제외)
템플릿)은 0점과 동일합니다.
• 헤더 파일을 서로 별도로 사용할 수 있어야 합니다.
이것이 바로 필요한 모든 종속성을 포함해야 하는 이유입니다. 그러나 포함 가드를 사용하여 이중 포함 문제를 방지해야 합니다. 그렇지 않으면 점수는 0이 됩니다.

나를 읽어주세요
• 필요한 경우 추가 파일을 렌더링할 수 있습니다(예: 코드를 더 많은 파일로 분리). 귀하의 작업은 프로그램에 의해 평가되지 않으므로 파일을 필수로 설정하는 한 최선이라고 생각하는 작업을 수행하십시오.
• 연습에 대한 지침은 간단해 보일 수 있지만 예제에는 명시적으로 요청되지 않은 추가 지침이 포함되어 있는 경우도 있습니다.
• 시작하기 전에 각 모듈을 완전히 읽으십시오! 정말.
• 오딘과 토르의 작품! 당신의 두뇌를 사용하십시오! ! !
많은 클래스를 구현해야 합니다.
어렵다는 것을 증명하세요... 아니면 안 됩니다! 즐겨 사용하는 텍스트 편집기를 사용하면 인생을 더 쉽게 만들 수 있는 방법이 있을 수 있습니다.
연습문제를 어떻게 해결하느냐는 당신의 자유입니다.
그러나 지침을 따르고 최소한만 준수하지 마십시오. 흥미로운 개념을 놓칠 수 있습니다.
이론을 자유롭게 읽어보세요.
